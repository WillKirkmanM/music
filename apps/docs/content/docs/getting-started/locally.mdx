---
title: Locally
description: Deploy it Locally
---
import { Callout } from 'fumadocs-ui/components/callout';
import { File, Files, Folder } from "fumadocs-ui/components/files";
import { Step, Steps } from "fumadocs-ui/components/steps"

<Steps>
  <Step>
    ### Install dependencies
    ```bash
    bun install
    ```
    <Callout title="Note">
      This will install all dependencies for all the apps and packages which includes `web`, `docs` and `landing`
    </Callout>
  </Step>
  
  <Step>
    ### Build music

    This will build the Web client for Music.

    ```bash
    bun build --filter music
    ``` 
  </Step>

  <Step>
    ### Run music

    This will build and run the server for Music, which hosts the Web client.

    ```bash
    cargo run -p music-server --release
    ```

    This will start music on port `3001`, to change the port, add the `--port` flag
  </Step>
</Steps>

<Callout title="View Music">View Music Running here: http://localhost:3001/</Callout>